describe('Describe test', () => {
  it('this should work', () => {
    expect(1).toBe(1)
  })
})

describe('Second Describe test', () => {
  it(`this should fail`, () => {
    expect(1).toBe(2)
  })
})
